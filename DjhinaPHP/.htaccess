Options -Indexes
RewriteEngine On

# Serve real files/directories directly (uploads, admin assets)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route everything else to index.php
RewriteRule ^ index.php [QSA,L]
